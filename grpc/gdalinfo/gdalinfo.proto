syntax = "proto3";

package gdalinfo;

message Request {
    string filePath = 1;
}

message GDALFile {
    string fileName = 1;
    string output = 2;
}

service GDALInfo {
    rpc Extract (Request) returns (GDALFile);
}
